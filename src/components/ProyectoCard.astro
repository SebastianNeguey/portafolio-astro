--- 
const { titulo, descripcion, imagenUrl, link } = Astro.props;
---

<div class="proyecto">
  <img src={`${Astro.base}${imagenUrl}`} alt={titulo} /> 
  <h3>{titulo}</h3>
  <p>{descripcion}</p>
  <a href={link} target="_blank">Ver más</a>
</div>
